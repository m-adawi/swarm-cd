---
{{- define "makeimage" -}}
  {{ . | replace "a" "o" }}:latest
{{- end }}

{{- define "nfs_volume" -}}
  {{ .name | replace "." "-" }}:
    driver_opts:
      type: nfs
      o: addr=1.2.3.4,nfsvers=4
      device: /path/to/{{ .name }}
{{- end }}

